"""Database-backed QC workspace domains.

The historical Django app label remains ``dashboard``.  Domain model modules
are imported by ``dashboard.models`` so migrations and content types keep that
stable identity while the implementation stays organized by capability.
"""

