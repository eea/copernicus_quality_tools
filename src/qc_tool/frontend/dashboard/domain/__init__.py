"""Database-backed QC workspace domains.

The Django app label is ``dashboard``. Domain model modules are imported by
``dashboard.models`` for model discovery while their implementation stays
organized by capability.
"""

